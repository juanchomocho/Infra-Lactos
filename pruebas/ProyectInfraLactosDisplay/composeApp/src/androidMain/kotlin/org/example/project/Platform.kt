package org.example.project

import android.content.Context

// Objeto para mantener una referencia global al contexto de la aplicación Android.
object AndroidAppContext {
    lateinit var context: Context
}
